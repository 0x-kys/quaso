width = 600
height = 400

# Colors
background_color = #0d0c0d
border_color = #5E5E5E
text_color = #FFFFFF

# Match the border size and color from Hyprland
border_width = 1
border = 1

# Rounding to match the Hyprland window decoration
rounding = 8

# Shadow to mimic Hyprland's window shadows
shadow = 1
shadow_color = #00000055
shadow_offset_x = 2
shadow_offset_y = 2

# Disable blur since it's not used in your Hyprland config
blur = 0

# Font settings (you might want to adjust according to your system's available fonts)
font = "Monospace 10"

# Keybindings and behavior
key_up = Up
key_down = Down
key_left = Left
key_right = Right
key_activate = Return
key_backspace = BackSpace

# Additional settings for behavior
allow_images = false
allow_markup = true
insensitive = true
